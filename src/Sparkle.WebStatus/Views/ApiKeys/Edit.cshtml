@model Sparkle.WebStatus.Domain.ApiKeyModel
@{
    ViewBag.Title = "Edit API key";
}

<section class="content">
<h2>@ViewBag.Title</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.EditorForModel()
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
</section>
